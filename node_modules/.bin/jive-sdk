#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jive-sdk/jive-sdk-service/generator/jive-sdk" "$@"
  ret=$?
else 
  node  "$basedir/../jive-sdk/jive-sdk-service/generator/jive-sdk" "$@"
  ret=$?
fi
exit $ret
